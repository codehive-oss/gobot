{
  "name": "@gobot/tsconfig",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "echo No need to build here!",
    "dev": "echo No need to dev here!",
    "clean": "rimraf .turbo && rimraf node_modules && rimraf dist"
  },
  "files": [
    "base.json",
    "esm.json",
    "nextjs.json",
    "node.json"
  ]
}
