{
  "name": "gobot",
  "version": "0.1.1",
  "repository": "https://github.com/SushiWaUmai/GoBot.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna version --conventional-commits -y",
    "diff": "lerna diff",
    "backend": "yarn workspace @gobot/backend",
    "frontend": "yarn workspace @gobot/frontend",
    "docker:build": "docker-compose build",
    "docker:dev": "docker-compose --env-file ./packages/backend/.env.development -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "docker:start": "docker-compose --env-file ./packages/backend/.env.production -f docker-compose.yml -f docker-compose.prod.yml up -d --build",
    "docker:stop": "docker-compose down"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
