version: "3.9"
services:
  backend:
    build:
      args:
        - BuildMode=production
    env_file:
      - ./packages/backend/.env.production
  frontend:
    build:
      args:
        - BuildMode=production